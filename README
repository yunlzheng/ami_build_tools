AWS AMI build tools
=========

## How to use it

```
export AWS_ACCESS_KEY= <your access key>
export AWS_SECRET_KEY= <your secret key>
```

```
./build.sh
```

You can use shell custom your ami by modify the scripts ansible.sh
